# M5 —

_Cohort notes for the M5 module. Edited as I work through it._

## Task 1 — Add tags key to note dict

**What I did:** Added `"tags": []` to the note dict inside `new_note()` in `app.py`.

**Why it matters:** Every note now carries a tags list from the moment it's created, so later code can always rely on the key existing — no `note.get("tags", [])` defensive checks needed downstream.

**Verified:** All existing task_01 tests still pass; a freshly created note has `tags == []`.

## Task 2 — Accept comma-separated tags in the form

**What I did:**
- Added a Tags input field to `templates/new_note.html` with `value="{{ tags or '' }}"` so typed values survive a validation failure.
- In `app.py`'s `new_note()` view, parsed the raw field with a one-liner:
  ```python
  tags = [t.strip() for t in tags_raw.split(",") if t.strip()]
  ```
  This trims whitespace from each tag and drops empty strings in one pass.

**Verified:**
- `"work, urgent"` → `["work", "urgent"]`
- `" foo ,  , bar , "` → `["foo", "bar"]` (spaces trimmed, empties removed)
- Empty field → `[]`
- All task_01 tests still pass.

## Task 3 —
